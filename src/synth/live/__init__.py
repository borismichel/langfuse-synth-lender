"""Live analyst-copilot playground: submit a question against a case and get a real
answer back (the pinned ``production`` prompt + a selectable model), emitted as a
native-looking agent-graph trace at *now*. ``/dossier`` renders the in-scene
validation memo (spec §18)."""
