# Model Validation Memo — Analyst Copilot
**Meridian Commercial Bank · Model Risk Management (second line)**

| | |
|---|---|
| **Candidate under review** | `{{ candidate_a_model }}` + prompt `{{ prompt_name }}` v{{ prompt_versions.production }} (temperature 0) |
| **Also evaluated** | `{{ candidate_b_model }}` (cost-reduction option) — **rejected**, see §3 |
| **Production reference** | `{{ incumbent_model }}` (baseline run {{ baseline.date }}) |
| **Validation date** | {{ run_date[:10] }} |
| **Method** | One certification suite ({{ suite_info["items"] }} items, scenario-gated); deterministic assertions + managed LLM judges ({{ judge_model }}) + human review |
| **Records** | Langfuse project **{{ project_name }}**{% if traces_link %} — [traces]({{ traces_link }}) · [datasets]({{ datasets_link }}) · [prompt]({{ prompts_link }}){% endif %} |

## 1 · Scope — the certified unit is the release
Certification covers **prompt + parameters + model**. Generations link to the exact
prompt version live at their timestamp; runs record the version they certified against
(v{{ prompt_versions.production }}). Any change to model, prompt, or suite re-enters this pipeline.

## 2 · Suite & gates
`{{ suite_info.name }}` — curated from annotated production traces (items carry
`sourceTraceId`; ground truth via the `{{ queue.name }}` review queue: {{ queue.completed }}
completed reviews, {{ queue.pending }} pending).

| Scenario | Items | Gate (deterministic check) |
|---|---|---|
{% for s, n in suite_info.scenarios.items() -%}
| {{ s }} | {{ n }} | ≥ {{ '%.0f%%'|format(gates.get(s, 0.95)*100) }} |
{% endfor %}

## 3 · The comparison (one suite, {{ runs_list|length }} runs)

| Run | Model | Date | numeric_lookup | groundedness (judge μ) | Verdict |
|---|---|---|---|---|---|
{% for r in runs_list -%}
| `{{ r.run_name }}` | {{ r.model }} | {{ r.date }} | {{ '%.1f%%'|format(((r.pass_rates or {}).get('numeric_lookup', 1))*100) }} | {{ r.groundedness_mu if r.groundedness_mu is defined else '—' }} | {{ {'baseline': 'baseline — passes', 'pass': '**PASS** (recommend)', 'fail': '**REJECTED** — numeric accuracy below gate', 'live': 'live run'}.get(r.verdict, r.verdict) }} |
{% endfor %}

Findings: candidate A matches the baseline on every gate with higher groundedness and
lower cost per answer; candidate B fails `numeric_lookup`
({{ '%.1f%%'|format(((candidate_b.pass_rates or {}).get('numeric_lookup', 0))*100) }} vs ≥{{ '%.0f%%'|format(gates.get('numeric_lookup', 0.95)*100) }}) —
each failing item's `numeric_accuracy` comment states the exact figure that diverged
from the printed table.

## 4 · Evidence trail
All five score-method types are present as score objects on one surface: deterministic
assertions (`numeric_accuracy`, `citation_format`, `escalation_correctness`), managed
judges (`groundedness`, `citation_coverage`), human annotation (`reviewer_verdict`),
and user feedback (`analyst_feedback`) — across production traces and experiment runs.
Judge–human agreement on co-annotated traces is ~88% with documented disagreements.
{% if workbench_runs %}
### Validation Workbench record
| Run | Spec | Release | Gates | Sign-off |
|---|---|---|---|---|
{% for w in workbench_runs -%}
| `{{ w.run_id }}` | `{{ w.spec_ref }}` (`{{ w.spec_hash[:12] }}…`) | {{ w.model }} + v{{ w.prompt_version }} | {{ 'PASS' if w.gates_ok else 'FAIL' }} | {{ w.signed_by or '—' }}{{ (' · ' + w.signed_at) if w.signed_at else '' }} |
{% endfor %}
{% endif %}

## 5 · Verdict
**Recommend certification of `{{ candidate_a_model }}` + `{{ prompt_name }}` v{{ prompt_versions.production }}.**
Candidate B is rejected on the numeric-accuracy gate; the failed run is retained as
evidence. Recertification triggers: model, prompt, parameter, or suite change; the
production monitors (judge samples + assertions + feedback) feed the next cycle.

*Generated by `synth memo` from run state — anchors are this run's real data.*
